name: Release

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

env:
  out-dir: ./out/
  artifacts-dir: ./artifacts/

jobs:
  build:
    runs-on: windows-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2
    - name: Fetch all history for all tags and branches
      run: |
        git config remote.origin.url https://x-access-token:${{ secrets.GITHUB_TOKEN }}@github.com/${{ github.repository }}
        git fetch --prune --unshallow
    - name: Setup .NET Core
      uses: actions/setup-dotnet@v1
      with:
        dotnet-version: 3.1.x
    - name: Install GitVersion
      uses: gittools/actions/gitversion/setup@v0.9.3
      with:
          versionSpec: '5.2.x'
    - name: Install dependencies
      run: dotnet restore
      working-directory: ./src
    - name: Publish
      run: dotnet publish -o "../${{ out-dir }}" -c Release --self-contained false --no-restore
      working-directory: ./src
    - name: Versioning
      id: gitversion
      uses: gittools/actions/gitversion/execute@v0.9.3
    - name: Zip
      run: |
        md -Force ${{ artifacts-dir }}
        Compress-Archive -Path ${{ out-dir }}/* -DestinationPath ${{ artifacts-dir }}giti-${{ steps.gitversion.outputs.fullSemVer }}.zip
      shell: pwsh
    - name: Release
      id: release
      uses: actions/create-release@v1.1.1
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      with:
        tag_name: ${{ steps.gitversion.outputs.fullSemVer }}
        release_name: ${{ steps.gitversion.outputs.fullSemVer }}
        draft: true
    - name: Upload
      id: upload
      uses: actions/upload-release-asset@v1.0.2
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      with:
        upload_url: ${{ steps.release.outputs.upload_url }}
        asset_path: ${{ artifacts-dir }}giti-${{ steps.gitversion.outputs.fullSemVer }}.zip
        asset_name: giti-${{ steps.gitversion.outputs.fullSemVer }}.zip
        asset_content_type: application/zip
